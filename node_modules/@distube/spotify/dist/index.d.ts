import { CustomPlugin, PlayOptions, SearchResult } from 'distube';
import { VoiceBasedChannel } from 'discord.js';

declare type SpotifyPluginOptions = {
    api?: {
        clientId: string;
        clientSecret: string;
    };
    parallel?: boolean;
    emitEventsAfterFetching?: boolean;
};
declare class SpotifyPlugin extends CustomPlugin {
    parallel: boolean;
    emitEventsAfterFetching: boolean;
    constructor(options?: SpotifyPluginOptions);
    validate(url: string): Promise<boolean>;
    play(voiceChannel: VoiceBasedChannel, url: string, options: PlayOptions): Promise<void>;
    search(query: string): Promise<SearchResult | null>;
}

export { SpotifyPlugin, SpotifyPlugin as default };
